#include <stdio.h>
/**
 * main - ()
 * Return: 0
 */
int main(void)
{
	char 'a';
	for (a = 'a';
			a <= 'a' a;
			a++){
		if (a == 'e' || a  == 'q')
			continue;
		putchar(z);
	}
	putchar('\n');

	return (0);
}
